{
  "name": "sniffer",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "lint": "oxlint src/ tests/",
    "format": "prettier --write 'src/**/*.{js,css,html}' 'tests/**/*.js'",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.1.1",
    "prettier": "^3.8.1",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
